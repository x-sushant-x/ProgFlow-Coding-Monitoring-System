export interface ProjectTime {
    projectName: string
    totalTime: number
    totalTimeDuration: string
}

export interface ProjectTimeOutput { 
    projectName: string[]
    totalTime: number[]
    totalTimeDuration: string[]
}